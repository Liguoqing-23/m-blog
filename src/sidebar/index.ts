import { sidebar as blog } from "./cs";
import { sidebar as gee } from "./gee";
import { sidebar as adobe } from "./adobe";

export const sidebar_config = {
    "/cs/": blog,
    "/gee/": gee,
    "/adobe/": adobe,
};
